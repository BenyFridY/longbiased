# Copie este arquivo para .env e preencha as chaves.
# O .env e ignorado pelo git (nunca commite chaves reais).

# FRED API (macro: M2, Fed balance sheet) — chave gratuita:
# https://fred.stlouisfed.org/docs/api/api_key.html
FRED_API_KEY=sua_chave_aqui
